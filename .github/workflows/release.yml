name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install tools
        run: |
          brew install xcodegen create-dmg

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Build
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          xcodebuild -project Tapistry.xcodeproj \
            -scheme Tapistry \
            -configuration Release \
            -derivedDataPath build \
            CODE_SIGN_IDENTITY="-" \
            MARKETING_VERSION="$VERSION"

      - name: Create DMG
        run: |
          APP_PATH="build/Build/Products/Release/Tapistry.app"
          DMG_NAME="Tapistry-${GITHUB_REF_NAME}.dmg"

          create-dmg \
            --volname "Tapistry" \
            --background "installer/dmg_background.png" \
            --window-pos 200 120 \
            --window-size 660 400 \
            --icon-size 120 \
            --icon "Tapistry.app" 490 180 \
            --app-drop-link 170 180 \
            --no-internet-enable \
            "$DMG_NAME" \
            "$APP_PATH"

          echo "DMG_NAME=$DMG_NAME" >> $GITHUB_ENV

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.DMG_NAME }}
          generate_release_notes: true
          body: |
            ## Tapistry ${{ github.ref_name }}

            macOS menu bar app — type to weave a tiny isometric village.

            ### Install
            1. Download `Tapistry-${{ github.ref_name }}.dmg`
            2. Open the DMG and drag **Tapistry** to **Applications**
            3. Open **Tapistry** — macOS will show an "unidentified developer" warning
            4. Go to **System Settings → Privacy & Security** and click **Open Anyway**
            5. Grant **Input Monitoring** permission when prompted

            ### Note
            This app is ad-hoc signed (not notarized with Apple Developer ID).
            You only need to allow it once via "Open Anyway" on first launch.
